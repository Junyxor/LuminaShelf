{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core APIs and native download notifications for the LuminaShelf main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
